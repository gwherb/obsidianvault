Want to use JEPA to train a video encoder with creates semantically rich representations.

These representations can then be used by smaller, fine-tuned models for downstream tasks (motion classification, action classification).

This process is more efficient compared to created multiple large models for each task.